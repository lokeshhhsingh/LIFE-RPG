export default function LandingPage() {
  return (
    <section>
      {/*
        Structural placeholder only — Teammate A owns visual design here.
        Keep the semantic tags (h1, section) intact when styling: they
        matter for SEO and screen readers.
      */}
      <h1>Life RPG</h1>
      <p>Turn your to-do list into a game.</p>
    </section>
  );
}
