# Set to "true" while backend endpoints aren't ready yet, "false" once wired up
NEXT_PUBLIC_USE_MOCK_API=true

# Base URL of the backend API once live (leave blank while mocking)
NEXT_PUBLIC_API_BASE_URL=
